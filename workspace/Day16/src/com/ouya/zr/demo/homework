1.定义一个Employee类
该类包括：private 成员变量name,age,birthday为MyDate类的对象；
并为每一个属性定义getter,setter方法；
并重写toString方法输出name,age,birthday

MyDate类包含：
private成员变量month,day.year；并为每一个属性定义getter,setter方法；

创建该类的5个对象，并把这些对象放入TreeSet集合中
分别按照以下两种方式对集合中的元素进行排序，并遍历输出：
1）使Employee实现Comparable接口，并按name排序
2）创建TreeSet时传入Comparator对象，按生日日期先后排序。